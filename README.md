# Interactief rad voor GitHub Pages

Plaats `index.html` in de hoofdmap van je repository. Laat `rad.png` staan in de map `assets`.

Correcte structuur:

```text
jouw-repository/
├─ index.html
└─ assets/
   └─ rad.png
```

Open daarna de GitHub Pages URL van de repository.
